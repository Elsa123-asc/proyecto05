public class Fichero {
    public void leer (String nombre fichero)
    valor = 1
    FileInputStream fis  = new FileInputStream (nombreFichero);
    BytesArrayOutputStream baos = new BytesArrayOutputStream ();
    while (valor = fis read ()) ! = -1) {
        baos.Write (valor);
    }

    retur.baos.toByteArray ();

     
}
